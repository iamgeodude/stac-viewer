{
  "name": "stac-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "rm -rf .svelte-kit/output build && BASE_PATH=/stac-viewer vite build && cp build/index.html build/404.html && touch build/.nojekyll && gh-pages -d build -t -b gh-pages"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.8.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "gh-pages": "^6.3.0",
    "svelte": "^5.1.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  }
}
